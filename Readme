O Banco de dados usado no projeto é o MySQL, as configurações estão no aplication.yml
-mysql://localhost:3306/

Os comandos para criação do banco e tabelas constam no arquivo "DB-CLIENT-CREATE-INSERTS.sql" e descritos também abaixo:

